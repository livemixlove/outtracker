describe('outage tracker end outage', () => {
    it('should pass', () => {
       
    })
    // it('should provide a summary of an outage with a single describe message', () => {
    //     sdfsdf()
    // })

    // it('should provide a summary of an outage with a 2 describe messages', () => {
    //     sdfsdf()
    // })

    // it('should provide a summary of an outage with a 1 recorded message', () => {
    //     sdfsdf()
    // })

    // it('should provide a summary of an outage with a both recorded and describe messages', () => {
    //     sdfsdf()
    // })
})